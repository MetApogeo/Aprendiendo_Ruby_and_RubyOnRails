services:
  web:
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/rails # <--- ¡ESTA ES LA MAGIA! Conecta Windows con Docker
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: development